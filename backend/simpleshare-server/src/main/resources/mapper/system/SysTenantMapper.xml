<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simpleshare.system.mapper.SysTenantMapper">

    <resultMap type="com.simpleshare.common.core.domain.entity.SysTenant" id="SysTenantResult">
        <result property="tenantId"        column="tenant_id"         />
        <result property="tenantCode"      column="tenant_code"       />
        <result property="tenantName"      column="tenant_name"       />
        <result property="contactUserName" column="contact_user_name" />
        <result property="contactPhone"    column="contact_phone"     />
        <result property="companyName"     column="company_name"      />
        <result property="licenseNumber"   column="license_number"    />
        <result property="address"         column="address"           />
        <result property="domain"          column="domain"            />
        <result property="intro"           column="intro"             />
        <result property="remark"          column="remark"            />
        <result property="status"          column="status"            />
        <result property="delFlag"         column="del_flag"          />
        <result property="createBy"        column="create_by"         />
        <result property="createTime"      column="create_time"       />
        <result property="updateBy"        column="update_by"         />
        <result property="updateTime"      column="update_time"       />
    </resultMap>

    <sql id="selectTenantVo">
        select tenant_id, tenant_code, tenant_name, contact_user_name, contact_phone, company_name, license_number, address, domain, intro, remark, status, del_flag, create_by, create_time, update_by, update_time from sys_tenant
    </sql>

    <select id="selectTenantList" parameterType="com.simpleshare.common.core.domain.entity.SysTenant" resultMap="SysTenantResult">
        <include refid="selectTenantVo"/>
        <where>
            <if test="tenantCode != null and tenantCode != ''"> and tenant_code like concat('%', #{tenantCode}, '%')</if>
            <if test="tenantName != null and tenantName != ''"> and tenant_name like concat('%', #{tenantName}, '%')</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
        </where>
        order by create_time desc
    </select>

    <select id="selectTenantById" parameterType="Long" resultMap="SysTenantResult">
        <include refid="selectTenantVo"/>
        where tenant_id = #{tenantId}
    </select>

    <select id="selectTenantByCode" parameterType="String" resultMap="SysTenantResult">
        <include refid="selectTenantVo"/>
        where tenant_code = #{tenantCode}
    </select>

    <select id="selectTenantByDomain" parameterType="String" resultMap="SysTenantResult">
        <include refid="selectTenantVo"/>
        where domain = #{domain}
    </select>

    <select id="checkTenantCodeUnique" resultMap="SysTenantResult">
        <include refid="selectTenantVo"/>
        where tenant_code = #{tenantCode} and tenant_id != #{tenantId} limit 1
    </select>

    <select id="checkTenantNameUnique" resultMap="SysTenantResult">
        <include refid="selectTenantVo"/>
        where tenant_name = #{tenantName} and tenant_id != #{tenantId} limit 1
    </select>

    <select id="selectActiveTenantList" resultMap="SysTenantResult">
        <include refid="selectTenantVo"/>
        where status = '0' and del_flag = '0'
        order by create_time desc
    </select>

    <insert id="insertTenant" parameterType="com.simpleshare.common.core.domain.entity.SysTenant" useGeneratedKeys="true" keyProperty="tenantId">
        insert into sys_tenant
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="tenantCode != null and tenantCode != ''">tenant_code,</if>
            <if test="tenantName != null and tenantName != ''">tenant_name,</if>
            <if test="contactUserName != null and contactUserName != ''">contact_user_name,</if>
            <if test="contactPhone != null and contactPhone != ''">contact_phone,</if>
            <if test="companyName != null and companyName != ''">company_name,</if>
            <if test="licenseNumber != null and licenseNumber != ''">license_number,</if>
            <if test="address != null and address != ''">address,</if>
            <if test="domain != null and domain != ''">domain,</if>
            <if test="intro != null and intro != ''">intro,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="tenantCode != null and tenantCode != ''">#{tenantCode},</if>
            <if test="tenantName != null and tenantName != ''">#{tenantName},</if>
            <if test="contactUserName != null and contactUserName != ''">#{contactUserName},</if>
            <if test="contactPhone != null and contactPhone != ''">#{contactPhone},</if>
            <if test="companyName != null and companyName != ''">#{companyName},</if>
            <if test="licenseNumber != null and licenseNumber != ''">#{licenseNumber},</if>
            <if test="address != null and address != ''">#{address},</if>
            <if test="domain != null and domain != ''">#{domain},</if>
            <if test="intro != null and intro != ''">#{intro},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateTenant" parameterType="com.simpleshare.common.core.domain.entity.SysTenant">
        update sys_tenant
        <trim prefix="SET" suffixOverrides=",">
            <if test="tenantCode != null and tenantCode != ''">tenant_code = #{tenantCode},</if>
            <if test="tenantName != null and tenantName != ''">tenant_name = #{tenantName},</if>
            <if test="contactUserName != null and contactUserName != ''">contact_user_name = #{contactUserName},</if>
            <if test="contactPhone != null and contactPhone != ''">contact_phone = #{contactPhone},</if>
            <if test="companyName != null and companyName != ''">company_name = #{companyName},</if>
            <if test="licenseNumber != null and licenseNumber != ''">license_number = #{licenseNumber},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="domain != null and domain != ''">domain = #{domain},</if>
            <if test="intro != null and intro != ''">intro = #{intro},</if>
            <if test="remark != null and remark != ''">remark = #{remark},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </trim>
        where tenant_id = #{tenantId}
    </update>

    <delete id="deleteTenantById" parameterType="Long">
        delete from sys_tenant where tenant_id = #{tenantId}
    </delete>

    <delete id="deleteTenantByIds" parameterType="String">
        delete from sys_tenant where tenant_id in
        <foreach item="tenantId" collection="array" open="(" separator="," close=")">
            #{tenantId}
        </foreach>
    </delete>

</mapper>

